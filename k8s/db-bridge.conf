worker_processes 1;
events { worker_connections 128; }
stream {
    resolver 127.0.0.11 valid=5s ipv6=off;
    upstream database { zone database 64k; server db:5432 resolve; }
    server { listen 5432; proxy_pass database; proxy_connect_timeout 5s; proxy_timeout 1h; }
}
