# Bug hunt status sweep — 12 September 2026

Status checks only; no fixes in this lane. All fixtures are prefixed ZZ. Root verification report records final cleanup.

| Area | Status and evidence | Worth building |
| --- | --- | --- |
| Team, roles and permissions | **PARTIAL.** Five live fixture roles exercised server routes. Owner has full access; Manager (`admin`) cannot invite admins (403); Marketing can manage contacts/marketing/website and read offers/products/reports; Support reads contacts/orders/offers/products and can request refunds; Coach accesses coaching. Disallowed module requests returned 403. However a fresh Coach (admin user 10) retrieved existing session 11, including `Private: watch cashflow.`, without an assignment: the claimed own-record restriction is not enforced by the legacy coaching-session route. | **High priority:** enforce coach ownership server-side. This is a privacy gap, not merely hidden navigation. Left unchanged as explicitly requested for this status lane. |
| Webhooks / Connections | **PRESENT.** A synthetic `test.ping` POST reached an HTTPS echo receiver: delivery 1, HTTP 200, one attempt, receiver echoed JSON and `X-BC-Signature`, event and delivery headers. A separate endpoint returned 503; delivery 2 automatically advanced from one to two attempts and scheduled another retry. Delivery detail exposes payload, response and errors; replay queued delivery 3. Existing endpoint list was empty. | No basic build needed. The 13 configured event names are below; only `test.ping` was exercised end to end in this sweep. Individual business-event emission remains unverified here. |
| Podcast RSS | **PARTIAL.** Public feed parses as RSS 2.0 but contains zero episodes; artwork is 1365 × 768. A temporary private feed returned 403 without/with invalid token, 200 with a valid token and 403 after revocation. See [podcast evidence](BUG-HUNT-PODCAST-STATUS.md). | Finish feed content/artwork and validate actual enclosure playback and directory submission. |
| Newsletter send | **PARTIAL.** Newsletter 3 / issue 3 was sent through the visible **Send → Send it now** flow to exactly one approved address, using a temporary paid-plan audience. Reload showed Sent / recipientCount 1. Gmail message `1a097153a6f9beb2` was opened: correct staging sender, SPF/DKIM/DMARC pass, expected content and unsubscribe footer. It uses the shared SES `sendEmail` path. Publishing the newsletter makes its product visible; the issue's Send action sends email. Issue status changes before detached delivery finishes; per-issue delivery attribution is absent (`broadcast`, null source ID). | Add durable issue jobs and truthful per-issue delivery/failure reporting. The actual sending path exists. |
| Form double opt-in | **PARTIAL.** A saved `doubleOptIn=true` form accepted submission 15, immediately subscribed contact 65, left `confirmed_at` null and produced zero email sends. No confirmation flow or visible builder control. See [forms evidence](BUG-HUNT-FORMS-STATUS.md). | Build the confirmation workflow and connect the setting to it. |
| Form embed snippet | **ABSENT.** Builder exposes a public form link but no embed/copy-code flow; inspected implementation has no supported embed endpoint. | Useful for lead capture from external sites. |

Webhook event catalogue: `contact.created`, `contact.updated`, `contact.tagged`, `order.paid`, `order.refunded`, `subscription.started`, `subscription.cancelled`, `member.created`, `member.granted_access`, `form.submitted`, `coaching.booked`, `coaching.cancelled`, `test.ping`. Delivery retries use persisted due times, up to five attempts; inspected backoff is 30/120/600/3600 seconds. Receiver tests contained only the generic test ping, no customer data. Test endpoints 1 and 2 were deleted after evidence capture, removing their pending retries.

Raw evidence under `/tmp/boss-bughunt`: `roles-result.json`, `role-scope.json`, `webhook-deliveries.json`, `webhook-echo.json`, `newsletter-sent.png`, `newsletter-fixtures.json`, plus linked lane reports. These temporary paths are copied into the final report's durable evidence folder where appropriate.
