# Dependabot version updates.
#
# Security updates are switched on for the repository and need no config; this
# file adds routine weekly version updates. Minor and patch bumps are grouped so
# an ordinary week is one pull request per ecosystem. Every pull request runs
# the full CI suite (including dependency review and the CodeQL gate), and
# nothing reaches production until it is merged to main.
#
# At most two open version-update pull requests per ecosystem. CI runs on
# GitHub-hosted runners with limited concurrency, shared with the CI that gates
# production deploys; the first run of this file opened fifteen at once and a
# deploy waited behind their builds.
version: 2
updates:
  - package-ecosystem: npm
    directory: /backend
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 2
    groups:
      backend-minor-and-patch:
        update-types: [minor, patch]

  - package-ecosystem: npm
    directory: /frontend
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 2
    groups:
      frontend-minor-and-patch:
        update-types: [minor, patch]

  - package-ecosystem: pip
    directory: /ai
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 2
    groups:
      ai-minor-and-patch:
        update-types: [minor, patch]

  # Base images in backend/Dockerfile and ai/Dockerfile. A new Node or Python
  # major is a runtime migration for the whole app, not a routine bump, so it
  # is left to a deliberate change.
  - package-ecosystem: docker
    directories: [/backend, /ai]
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 2
    ignore:
      - dependency-name: node
        update-types: [version-update:semver-major]
      - dependency-name: python
        update-types: [version-update:semver-major, version-update:semver-minor]

  # postgres, nginx and coturn in docker-compose.yml. A Postgres major needs a
  # dump and restore, never an image bump.
  - package-ecosystem: docker-compose
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 2
    ignore:
      - dependency-name: postgres
        update-types: [version-update:semver-major]

  # Actions are pinned to commit SHAs; Dependabot moves the SHA and the version
  # comment together.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 2
    groups:
      actions:
        patterns: ["*"]
