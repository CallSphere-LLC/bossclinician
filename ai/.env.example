OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5-2026-04-23
# Optional: only set if routing through a proxy/gateway instead of OpenAI directly.
OPENAI_BASE_URL=

# Optional overrides (sensible defaults baked into app/config.py):
# AI_KB_PATH=knowledge/site.md
# AI_KB_MAX_CHARS=24000
# CORS_ALLOW_ORIGINS=*
