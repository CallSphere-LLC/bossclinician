fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
openai==3.15.0
# HTTP client for the Realtime client-secret call, and what the OpenAI SDK
# (3.x) and Starlette's TestClient use. openai no longer installs legacy httpx.
httpx2==2.13.0
python-dotenv==1.2.3

# dev/test only (not needed at runtime; harmless in the image)
pytest==9.1.1
